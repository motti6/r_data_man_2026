{
  "hash": "9e91ed60db7b8d54bb775417d1a0f50b",
  "result": {
    "engine": "knitr",
    "markdown": "---\ntitle: \"第5章 ggplot2による実データ可視化\"\nsubtitle: \"分布、関係、推移、ランキングを伝える\"\n---\n\n::: {.callout-note}\n## この章の到達目標\n\n1. 分析目的と変数の型に応じて、適切なグラフを選べる。\n2. `ggplot()`、`aes()`、`geom_*()`の役割を説明できる。\n3. 賃貸物件、貿易、国際収支、WEO、都道府県GDPを可視化できる。\n4. 軸、単位、凡例、注記、出所を整え、第三者に伝わる図を作れる。\n5. 異なる単位の系列を、安易な二軸グラフではなく、指数化やファセットで比較できる。\n6. 図をオブジェクトとして保存し、再現可能な形式で出力できる。\n:::\n\n\n\n# グラフを描く前に決めること\n\n1. **何を伝えるか**：分布、比較、関係、推移のどれか。\n2. **観測単位は何か**：物件、国、都道府県、月など。\n3. **変数の型は何か**：連続値、カテゴリ、日付など。\n4. **単位と対象期間は何か**。\n5. **探索用か、報告用か**。\n\n| 確認したいこと | 代表的なグラフ |\n|---|---|\n| カテゴリの件数 | 棒グラフ |\n| 連続値の分布 | ヒストグラム、密度図 |\n| 群ごとの分布 | 箱ひげ図、バイオリン図 |\n| 2変数の関係 | 散布図 |\n| 時系列の推移 | 折れ線、棒グラフ |\n| 複数群・複数系列 | 色、線種、ファセット |\n\n::: {.callout-important}\n## グラフは因果関係を自動的に示さない\n\n散布図に右上がりの傾向があっても、他の変数、逆方向の因果、データの選択によって生じた可能性があります。グラフは、データの特徴を発見し、仮説や分析結果を伝える道具です。\n:::\n\n# ggplot2の基本構造\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(\n  data = tx_df,\n  mapping = aes(x = size_sqm, y = monthly_rent)\n) +\n  geom_point()\n```\n\n::: {.cell-output-display}\n![](chapter5_2026_files/figure-html/unnamed-chunk-1-1.png){width=672}\n:::\n:::\n\n\n- `ggplot(data, aes(...))`：データと変数の対応を決める。\n- `geom_*()`：点、線、棒、箱などの形を加える。\n- `scale_*()`：軸、色、目盛り、ラベル形式を調整する。\n- `labs()`：タイトル、軸名、凡例、注記を加える。\n- `facet_*()`：小さな複数図へ分割する。\n- `theme_*()`：背景、文字、凡例位置などを整える。\n\n`+`は前の行の末尾に置きます。\n\n# カテゴリの件数：棒グラフ\n\n間取り別の物件数を確認します。\n\n\n::: {.cell}\n\n```{.r .cell-code}\ntx_df |>\n  count(layout, name = \"n\") |>\n  mutate(layout = fct_reorder(layout, n)) |>\n  ggplot(aes(x = layout, y = n)) +\n  geom_col() +\n  coord_flip() +\n  labs(\n    x = \"間取り\",\n    y = \"物件数\",\n    caption = \"資料：過去講義用の賃貸物件データ\"\n  ) +\n  theme_minimal(base_family = \"sans\")\n```\n\n::: {.cell-output-display}\n![間取り別の掲載物件数](chapter5_2026_files/figure-html/unnamed-chunk-2-1.png){width=672}\n:::\n:::\n\n\n`geom_bar()`は行数を内部で数えます。集計済みの値を棒の高さにするときは`geom_col()`を使います。\n\n上位の間取りだけを示す場合は、除外条件を注記します。\n\n\n::: {.cell}\n\n```{.r .cell-code}\nlayout_top10 <- tx_df |>\n  count(layout, sort = TRUE) |>\n  slice_head(n = 10) |>\n  mutate(layout = fct_reorder(layout, n))\n\nggplot(layout_top10, aes(x = layout, y = n)) +\n  geom_col() +\n  coord_flip() +\n  labs(\n    x = NULL,\n    y = \"物件数\",\n    caption = \"注：掲載件数上位10間取り。資料：過去講義用データ\"\n  ) +\n  theme_minimal(base_family = \"sans\")\n```\n\n::: {.cell-output-display}\n![掲載件数上位10間取り](chapter5_2026_files/figure-html/unnamed-chunk-3-1.png){width=672}\n:::\n:::\n\n\n# 連続値の分布：ヒストグラム\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(tx_df, aes(x = monthly_rent)) +\n  geom_histogram(binwidth = 1, boundary = 0) +\n  labs(\n    x = \"月額家賃\",\n    y = \"物件数\",\n    caption = \"資料：過去講義用の賃貸物件データ\"\n  ) +\n  theme_minimal(base_family = \"sans\")\n```\n\n::: {.cell-output-display}\n![月額家賃の分布](chapter5_2026_files/figure-html/unnamed-chunk-4-1.png){width=672}\n:::\n:::\n\n\n`binwidth`を変えると見え方も変わります。\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbind_rows(\n  tx_df |>\n    transmute(monthly_rent, bin = \"階級幅0.5\"),\n  tx_df |>\n    transmute(monthly_rent, bin = \"階級幅2.0\")\n) |>\n  ggplot(aes(x = monthly_rent)) +\n  geom_histogram(\n    data = ~ filter(.x, bin == \"階級幅0.5\"),\n    binwidth = 0.5\n  ) +\n  geom_histogram(\n    data = ~ filter(.x, bin == \"階級幅2.0\"),\n    binwidth = 2\n  ) +\n  facet_wrap(~ bin, scales = \"free_y\") +\n  labs(x = \"月額家賃\", y = \"物件数\") +\n  theme_minimal(base_family = \"sans\")\n```\n\n::: {.cell-output-display}\n![階級幅による家賃分布の見え方](chapter5_2026_files/figure-html/unnamed-chunk-5-1.png){width=672}\n:::\n:::\n\n\nこの例では2つの`geom_histogram()`を重ねていますが、各ファセットには対応するデータだけが描かれます。より簡潔にするなら、別々の図を作る方法もあります。\n\n# 群ごとの分布：箱ひげ図\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(tx_df, aes(x = line_group, y = monthly_rent)) +\n  geom_boxplot() +\n  labs(\n    x = NULL,\n    y = \"月額家賃\",\n    caption = \"資料：過去講義用の賃貸物件データ\"\n  ) +\n  theme_minimal(base_family = \"sans\")\n```\n\n::: {.cell-output-display}\n![TX沿線区分別の家賃分布](chapter5_2026_files/figure-html/unnamed-chunk-6-1.png){width=672}\n:::\n:::\n\n\n箱ひげ図は中央値と四分位範囲を比較できますが、群の件数を直接示しません。件数も併記します。\n\n\n::: {.cell}\n\n```{.r .cell-code}\ntx_df |>\n  count(line_group)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n# A tibble: 2 × 2\n  line_group     n\n  <chr>      <int>\n1 TX沿線       639\n2 その他      1117\n```\n\n\n:::\n:::\n\n\n間取りを限定して比較すると、構成差を少し抑えられます。\n\n\n::: {.cell}\n\n```{.r .cell-code}\ntx_df |>\n  filter(layout %in% c(\"1K\", \"1LDK\", \"2LDK\", \"3LDK\")) |>\n  ggplot(aes(x = line_group, y = monthly_rent)) +\n  geom_boxplot() +\n  facet_wrap(~ layout, scales = \"free_y\") +\n  labs(\n    x = NULL,\n    y = \"月額家賃\",\n    caption = \"資料：過去講義用の賃貸物件データ\"\n  ) +\n  theme_minimal(base_family = \"sans\")\n```\n\n::: {.cell-output-display}\n![主要間取りに限定した沿線区分別家賃分布](chapter5_2026_files/figure-html/unnamed-chunk-8-1.png){width=672}\n:::\n:::\n\n\n# 2変数の関係：散布図\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(tx_df, aes(x = size_sqm, y = monthly_rent)) +\n  geom_point(alpha = 0.35) +\n  geom_smooth(method = \"lm\", se = FALSE) +\n  labs(\n    x = \"専有面積\",\n    y = \"月額家賃\",\n    caption = \"資料：過去講義用の賃貸物件データ\"\n  ) +\n  theme_minimal(base_family = \"sans\")\n```\n\n::: {.cell-output-display}\n![専有面積と家賃](chapter5_2026_files/figure-html/unnamed-chunk-9-1.png){width=672}\n:::\n:::\n\n\n大きな物件が少数含まれるため、全体像が圧縮される場合があります。データを削除する前に、分位点を確認します。\n\n\n::: {.cell}\n\n```{.r .cell-code}\nquantile(tx_df$size_sqm, c(0.5, 0.9, 0.95, 0.99, 1))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n     50%      90%      95%      99%     100% \n 41.0500  70.4050  85.5000 111.1025 317.0000 \n```\n\n\n:::\n:::\n\n\n99パーセンタイル以下を拡大表示します。これはデータ削除ではなく、表示範囲を限定した別図です。\n\n\n::: {.cell}\n\n```{.r .cell-code}\nsize_p99 <- quantile(tx_df$size_sqm, 0.99)\n\nggplot(tx_df, aes(x = size_sqm, y = monthly_rent, shape = line_group)) +\n  geom_point(alpha = 0.45) +\n  geom_smooth(method = \"lm\", se = FALSE) +\n  coord_cartesian(xlim = c(0, size_p99)) +\n  labs(\n    x = \"専有面積\",\n    y = \"月額家賃\",\n    shape = \"区分\",\n    caption = \"注：x軸は面積の99パーセンタイルまでを表示。資料：過去講義用データ\"\n  ) +\n  theme_minimal(base_family = \"sans\")\n```\n\n::: {.cell-output-display}\n![専有面積99パーセンタイル以下の拡大図](chapter5_2026_files/figure-html/unnamed-chunk-11-1.png){width=672}\n:::\n:::\n\n\n`coord_cartesian()`は表示範囲を変えるだけで、回帰線の推計に使う観測を削除しません。分析対象を限定したい場合は`filter()`を使い、その理由を明示します。\n\n# 時系列：貿易統計\n\n輸出と輸入を縦持ちへ変換します。\n\n\n::: {.cell}\n\n```{.r .cell-code}\ntrade_long <- trade |>\n  pivot_longer(\n    cols = c(exports, imports),\n    names_to = \"series\",\n    values_to = \"value\"\n  ) |>\n  mutate(\n    series = recode(\n      series,\n      exports = \"輸出\",\n      imports = \"輸入\"\n    )\n  )\n```\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(trade_long, aes(x = date, y = value, linetype = series)) +\n  geom_line(linewidth = 0.7) +\n  scale_x_date(\n    date_breaks = \"2 years\",\n    date_labels = \"%Y\"\n  ) +\n  scale_y_continuous(\n    labels = scales::label_number(\n      scale = 1 / 1e6,\n      suffix = \"兆円\",\n      accuracy = 0.1\n    )\n  ) +\n  labs(\n    x = NULL,\n    y = \"通関額\",\n    linetype = NULL,\n    caption = \"注：百万円を兆円表示。季節調整値。資料：過去講義用の貿易統計スナップショット\"\n  ) +\n  theme_minimal(base_family = \"sans\") +\n  theme(legend.position = \"bottom\")\n```\n\n::: {.cell-output-display}\n![輸出入額の推移（季節調整値）](chapter5_2026_files/figure-html/unnamed-chunk-13-1.png){width=672}\n:::\n:::\n\n\n2020年前後を背景で示します。\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(trade_long, aes(x = date, y = value, linetype = series)) +\n  annotate(\n    \"rect\",\n    xmin = as.Date(\"2020-01-01\"),\n    xmax = as.Date(\"2020-12-31\"),\n    ymin = -Inf,\n    ymax = Inf,\n    alpha = 0.12\n  ) +\n  geom_line(linewidth = 0.7) +\n  scale_y_continuous(\n    labels = scales::label_number(scale = 1 / 1e6, suffix = \"兆円\")\n  ) +\n  labs(\n    x = NULL,\n    y = \"通関額\",\n    linetype = NULL,\n    caption = \"注：網掛けは2020年。資料：過去講義用の貿易統計スナップショット\"\n  ) +\n  theme_minimal(base_family = \"sans\") +\n  theme(legend.position = \"bottom\")\n```\n\n::: {.cell-output-display}\n![輸出入額の推移と2020年の期間表示](chapter5_2026_files/figure-html/unnamed-chunk-14-1.png){width=672}\n:::\n:::\n\n\n網掛けは出来事と因果関係を証明するものではなく、読者が時期を確認する補助です。\n\n## 貿易収支\n\n\n::: {.cell}\n\n```{.r .cell-code}\ntrade_balance <- trade |>\n  mutate(balance = exports - imports)\n```\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(trade_balance, aes(x = date, y = balance)) +\n  geom_col() +\n  geom_hline(yintercept = 0) +\n  scale_y_continuous(\n    labels = scales::label_number(scale = 1 / 1e6, suffix = \"兆円\")\n  ) +\n  labs(\n    x = NULL,\n    y = \"輸出－輸入\",\n    caption = \"注：百万円を兆円表示。資料：過去講義用の貿易統計スナップショット\"\n  ) +\n  theme_minimal(base_family = \"sans\")\n```\n\n::: {.cell-output-display}\n![貿易収支の推移](chapter5_2026_files/figure-html/unnamed-chunk-16-1.png){width=672}\n:::\n:::\n\n\n# 国際収支の構成を可視化する\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbop_long <- bop |>\n  select(date, current_account, trade_balance, services_balance, primary_income, secondary_income) |>\n  pivot_longer(\n    cols = -date,\n    names_to = \"component\",\n    values_to = \"value\"\n  )\n```\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbop_long |>\n  filter(date >= as.Date(\"2015-01-01\")) |>\n  ggplot(aes(x = date, y = value)) +\n  geom_line() +\n  geom_hline(yintercept = 0) +\n  facet_wrap(\n    ~ component,\n    scales = \"free_y\",\n    ncol = 1\n  ) +\n  labs(\n    x = NULL,\n    y = \"配布データの単位\",\n    caption = \"資料：財務省・日本銀行『国際収支統計』の講義用スナップショット\"\n  ) +\n  theme_minimal(base_family = \"sans\")\n```\n\n::: {.cell-output-display}\n![国際収支の主要項目](chapter5_2026_files/figure-html/unnamed-chunk-18-1.png){width=672}\n:::\n:::\n\n\n異なる振れ幅の系列をファセットし、各図で縦軸を変えています。系列間の絶対額を比較したい場合は、共通軸を使う必要があります。\n\n# 異なる単位を比較する：指数化\n\n輸出額、輸入額、経常収支は水準も符号も異なるため、経常収支を含む単純指数化は注意が必要です。ここでは正の値である輸出と輸入を、2019年平均=100へ指数化します。\n\n\n::: {.cell}\n\n```{.r .cell-code}\ntrade_index <- trade_long |>\n  group_by(series) |>\n  mutate(\n    base_2019 = mean(value[year(date) == 2019], na.rm = TRUE),\n    index_2019 = value / base_2019 * 100\n  ) |>\n  ungroup()\n```\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(trade_index, aes(x = date, y = index_2019, linetype = series)) +\n  geom_hline(yintercept = 100) +\n  geom_line(linewidth = 0.7) +\n  labs(\n    x = NULL,\n    y = \"2019年平均=100\",\n    linetype = NULL,\n    caption = \"資料：過去講義用の貿易統計スナップショット\"\n  ) +\n  theme_minimal(base_family = \"sans\") +\n  theme(legend.position = \"bottom\")\n```\n\n::: {.cell-output-display}\n![輸出入額の指数（2019年平均=100）](chapter5_2026_files/figure-html/unnamed-chunk-20-1.png){width=672}\n:::\n:::\n\n\n二軸グラフは、軸の取り方で関係が強く見えたり弱く見えたりします。指数化、前年差、標準化、ファセットを優先します。\n\n# 都道府県GDPのランキング\n\n2021年の都道府県別GDP上位15地域を描きます。\n\n\n::: {.cell}\n\n```{.r .cell-code}\ngdp_2021_top15 <- gdp_pref |>\n  filter(year == 2021) |>\n  slice_max(gdp, n = 15, with_ties = FALSE) |>\n  mutate(pref = fct_reorder(pref, gdp))\n```\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggplot(gdp_2021_top15, aes(x = pref, y = gdp)) +\n  geom_col() +\n  coord_flip() +\n  scale_y_continuous(\n    labels = scales::label_number(\n      scale = 1 / 1e6,\n      suffix = \"兆円\",\n      accuracy = 1\n    )\n  ) +\n  labs(\n    x = NULL,\n    y = \"gdp\",\n    caption = \"注：名目値、百万円を兆円表示。資料：内閣府『県民経済計算』講義用データ\"\n  ) +\n  theme_minimal(base_family = \"sans\")\n```\n\n::: {.cell-output-display}\n![都道府県別GDP上位15地域（2021年）](chapter5_2026_files/figure-html/unnamed-chunk-22-1.png){width=672}\n:::\n:::\n\n\nランキングは規模を示しますが、人口規模の影響を受けます。一人当たりの比較には人口データとの結合が必要です。\n\n# 第2章のAPIデータを使う\n\n第2章のAPI実習で`data_clean/pref_population_2020.csv`を作成した場合、2020年の一人当たりGDPを計算できます。APIをまだ実行していない場合も、この章の他の図は配布スナップショットだけで再現できます。\n\n\n::: {.cell}\n\n```{.r .cell-code}\nhas_api_population <- file.exists(\"data_clean/pref_population_2020.csv\")\nhas_api_population\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] FALSE\n```\n\n\n:::\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nif (has_api_population) {\n  population_2020 <- read_csv(\n    \"data_clean/pref_population_2020.csv\",\n    show_col_types = FALSE\n  )\n\n  gdp_per_capita_2020 <- gdp_pref |>\n    filter(year == 2020) |>\n    inner_join(\n      population_2020 |>\n        select(pref, population),\n      by = join_by(pref),\n      relationship = \"one-to-one\"\n    ) |>\n    mutate(\n      gdp_per_capita_10k_yen = gdp * 100 / population\n    ) |>\n    slice_max(gdp_per_capita_10k_yen, n = 15, with_ties = FALSE) |>\n    mutate(pref = fct_reorder(pref, gdp_per_capita_10k_yen))\n\n  ggplot(\n    gdp_per_capita_2020,\n    aes(x = pref, y = gdp_per_capita_10k_yen)\n  ) +\n    geom_col() +\n    coord_flip() +\n    labs(\n      x = NULL,\n      y = \"一人当たりGDP（万円）\",\n      caption = paste(\n        \"資料：内閣府『県民経済計算』、\",\n        \"e-Stat『人口推計』から作成\"\n      )\n    ) +\n    theme_minimal(base_family = \"sans\")\n} else {\n  message(\n    \"第2章でdata_clean/pref_population_2020.csvを作成すると、\",\n    \"一人当たりGDPの図を表示できます。\"\n  )\n}\n```\n:::\n\n\n`gdp`は百万円、人口は人なので、`gdp * 100 / population`で一人当たり万円になります。単位変換は式と注記の両方へ残します。\n\n# WEOで国際比較を描く\n\nWEOは、1行が1国・1指標、年が横方向に並ぶ形式です。必要な系列を縦長へ変換します。\n\n\n::: {.cell}\n\n```{.r .cell-code}\nweo_raw <- read_excel(\n  file.path(\"data_raw\", \"b_weo.xlsx\"),\n  sheet = \"weo\",\n  col_types = \"text\",\n  na = c(\"\", \"--\", \"...\", \"n/a\")\n)\n\nyear_columns <- names(weo_raw) |>\n  str_subset(\"^\\\\d{4}$\")\n\nweo_long <- weo_raw |>\n  pivot_longer(\n    cols = all_of(year_columns),\n    names_to = \"year\",\n    values_to = \"value_raw\"\n  ) |>\n  mutate(\n    year = parse_integer(year),\n    value = parse_number(value_raw)\n  )\n\ngroup_map <- read_excel(\n  file.path(\"data_raw\", \"b_weo.xlsx\"),\n  sheet = \"group_map\"\n) |>\n  select(iso3, group) |>\n  distinct(iso3, .keep_all = TRUE)\n```\n:::\n\n\n2023年の購買力平価GDPと一人当たりGDPを国単位で横持ちへします。\n\n\n::: {.cell}\n\n```{.r .cell-code}\nweo_2023 <- weo_long |>\n  filter(\n    year == 2023,\n    subject_code %in% c(\"PPPGDP\", \"PPPPC\", \"LP\")\n  ) |>\n  select(iso, country, subject_code, value) |>\n  pivot_wider(\n    names_from = subject_code,\n    values_from = value\n  ) |>\n  inner_join(\n    group_map,\n    by = join_by(iso == iso3),\n    relationship = \"many-to-one\"\n  )\n```\n:::\n\n\n地域GDPは国別GDPを合計できます。一人当たりGDPは単純合計せず、GDP合計を人口`LP`の合計で割ります。\n\n\n::: {.cell}\n\n```{.r .cell-code}\nweo_group_2023 <- weo_2023 |>\n  summarise(\n    countries = n(),\n    ppp_gdp_billion = sum(PPPGDP, na.rm = TRUE),\n    population_million = sum(LP, na.rm = TRUE),\n    ppp_per_capita = ppp_gdp_billion / population_million * 1000,\n    median_country_ppp_per_capita = median(PPPPC, na.rm = TRUE),\n    .by = group\n  )\n\nweo_group_2023\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n# A tibble: 3 × 6\n  group countries ppp_gdp_billion population_million ppp_per_capita\n  <chr>     <int>           <dbl>              <dbl>          <dbl>\n1 EU           24          13553.               238.         56938.\n2 ASEAN        10          11470.               680.         16873.\n3 G7            7          54339.               782.         69512.\n# ℹ 1 more variable: median_country_ppp_per_capita <dbl>\n```\n\n\n:::\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nweo_group_2023 |>\n  mutate(group = fct_reorder(group, ppp_gdp_billion)) |>\n  ggplot(aes(x = group, y = ppp_gdp_billion)) +\n  geom_col() +\n  coord_flip() +\n  scale_y_continuous(\n    labels = scales::label_number(big.mark = \",\")\n  ) +\n  labs(\n    x = NULL,\n    y = \"購買力平価GDP（10億国際ドル）\",\n    caption = \"資料：IMF, World Economic Outlook Database, April 2025\"\n  ) +\n  theme_minimal(base_family = \"sans\")\n```\n\n::: {.cell-output-display}\n![G7・EU（G7除く）・ASEANの購買力平価GDP（2023年）](chapter5_2026_files/figure-html/unnamed-chunk-27-1.png){width=672}\n:::\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nweo_group_2023 |>\n  mutate(group = fct_reorder(group, ppp_per_capita)) |>\n  ggplot(aes(x = group, y = ppp_per_capita)) +\n  geom_col() +\n  coord_flip() +\n  scale_y_continuous(\n    labels = scales::label_dollar(\n      prefix = \"\",\n      suffix = \" 国際ドル\",\n      big.mark = \",\",\n      accuracy = 1\n    )\n  ) +\n  labs(\n    x = NULL,\n    y = \"人口加重した一人当たりGDP\",\n    caption = \"資料：IMF, World Economic Outlook Database, April 2025\"\n  ) +\n  theme_minimal(base_family = \"sans\")\n```\n\n::: {.cell-output-display}\n![G7・EU（G7除く）・ASEANの一人当たり購買力平価GDP（2023年）](chapter5_2026_files/figure-html/unnamed-chunk-28-1.png){width=672}\n:::\n:::\n\n\nグループ対応表では、EUからG7加盟国を除いています。グループ定義は分析結果の一部なので、注記します。\n\n# ラベルと出所を整える\n\n報告用の図には、少なくとも次を含めます。\n\n- 軸名と単位\n- 対象期間\n- 季節調整・名目実質などの定義\n- データ加工上の重要な条件\n- 出所\n\nタイトルが長くなりすぎる場合は、本文で説明し、図には軸名と注記だけを置きます。\n\n# 図を保存する\n\n図をオブジェクトへ保存します。\n\n\n::: {.cell}\n\n```{.r .cell-code}\np_trade <- ggplot(\n  trade_long,\n  aes(x = date, y = value, linetype = series)\n) +\n  geom_line(linewidth = 0.7) +\n  scale_y_continuous(\n    labels = scales::label_number(scale = 1 / 1e6, suffix = \"兆円\")\n  ) +\n  labs(\n    x = NULL,\n    y = \"通関額\",\n    linetype = NULL,\n    caption = \"資料：過去講義用の貿易統計スナップショット\"\n  ) +\n  theme_minimal(base_family = \"sans\") +\n  theme(legend.position = \"bottom\")\n\np_trade\n```\n\n::: {.cell-output-display}\n![](chapter5_2026_files/figure-html/unnamed-chunk-29-1.png){width=672}\n:::\n:::\n\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggsave(\n  filename = file.path(\"figures\", \"trade_exports_imports.png\"),\n  plot = p_trade,\n  width = 10,\n  height = 5.5,\n  units = \"in\",\n  dpi = 300\n)\n```\n:::\n\n\nベクター形式が必要ならPDFやSVGを使います。\n\n\n::: {.cell}\n\n```{.r .cell-code}\nggsave(\n  filename = file.path(\"figures\", \"trade_exports_imports.pdf\"),\n  plot = p_trade,\n  width = 10,\n  height = 5.5,\n  units = \"in\"\n)\n```\n:::\n\n\n`plot = p_trade`を明示すると、最後に表示した別の図を誤って保存する事故を防げます。\n\n# 章末演習\n\n## 演習1：賃貸物件の説明図\n\n次の3枚を作成してください。\n\n1. 間取り別物件数の棒グラフ\n2. TX沿線区分別・主要間取り別の家賃箱ひげ図\n3. 面積と家賃の散布図。築年数をファセットにする\n\nすべてに軸名、単位、出所を付けます。\n\n::: {.callout-tip collapse=\"true\"}\n## 解答例：散布図\n\n\n::: {.cell}\n\n```{.r .cell-code}\ntx_plot <- tx_df |>\n  mutate(\n    age_group = factor(\n      age_group,\n      levels = c(\n        \"築5年以下\",\n        \"築6～15年\",\n        \"築16～30年\",\n        \"築31年以上\"\n      )\n    )\n  )\n\nggplot(tx_plot, aes(x = size_sqm, y = monthly_rent, shape = line_group)) +\n  geom_point(alpha = 0.35) +\n  geom_smooth(method = \"lm\", se = FALSE) +\n  facet_wrap(~ age_group) +\n  coord_cartesian(xlim = c(0, quantile(tx_plot$size_sqm, 0.99))) +\n  labs(\n    x = \"専有面積\",\n    y = \"月額家賃\",\n    shape = \"区分\",\n    caption = \"注：面積の99パーセンタイルまで表示。資料：過去講義用データ\"\n  ) +\n  theme_minimal(base_family = \"sans\")\n```\n\n::: {.cell-output-display}\n![](chapter5_2026_files/figure-html/unnamed-chunk-32-1.png){width=672}\n:::\n:::\n\n:::\n\n## 演習2：国際収支の図\n\n2015年以降について、経常収支、貿易収支、サービス収支、第一次所得収支を四半期合計へ変換し、ファセットで表示してください。未完了四半期は除外します。\n\n::: {.callout-tip collapse=\"true\"}\n## 解答例\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbop_quarter_plot <- bop |>\n  filter(date >= as.Date(\"2015-01-01\")) |>\n  mutate(quarter = floor_date(date, \"quarter\")) |>\n  summarise(\n    current_account = sum(current_account, na.rm = TRUE),\n    trade_balance = sum(trade_balance, na.rm = TRUE),\n    services_balance = sum(services_balance, na.rm = TRUE),\n    primary_income = sum(primary_income, na.rm = TRUE),\n    months = n(),\n    .by = quarter\n  ) |>\n  filter(months == 3) |>\n  select(-months) |>\n  pivot_longer(\n    cols = -quarter,\n    names_to = \"component\",\n    values_to = \"value\"\n  )\n\nggplot(bop_quarter_plot, aes(x = quarter, y = value)) +\n  geom_col() +\n  geom_hline(yintercept = 0) +\n  facet_wrap(~ component, scales = \"free_y\", ncol = 1) +\n  labs(\n    x = NULL,\n    y = \"四半期合計\",\n    caption = \"資料：財務省・日本銀行『国際収支統計』講義用データ\"\n  ) +\n  theme_minimal(base_family = \"sans\")\n```\n\n::: {.cell-output-display}\n![](chapter5_2026_files/figure-html/unnamed-chunk-33-1.png){width=672}\n:::\n:::\n\n:::\n\n## 演習3：2023年と2030年のWEO比較\n\nG7、EU（G7加盟国除く）、ASEANについて、購買力平価GDPを2023年と2030年で比較する図を作ってください。2030年が予測値であることを注記します。\n\n::: {.callout-tip collapse=\"true\"}\n## 解答例\n\n\n::: {.cell}\n\n```{.r .cell-code}\nweo_group_compare <- weo_long |>\n  filter(\n    subject_code == \"PPPGDP\",\n    year %in% c(2023, 2030)\n  ) |>\n  inner_join(\n    group_map,\n    by = join_by(iso == iso3),\n    relationship = \"many-to-one\"\n  ) |>\n  summarise(\n    ppp_gdp_billion = sum(value, na.rm = TRUE),\n    .by = c(group, year)\n  ) |>\n  mutate(year = factor(year))\n\nggplot(\n  weo_group_compare,\n  aes(x = group, y = ppp_gdp_billion, fill = year)\n) +\n  geom_col(position = \"dodge\") +\n  coord_flip() +\n  labs(\n    x = NULL,\n    y = \"購買力平価GDP（10億国際ドル）\",\n    fill = \"年\",\n    caption = paste(\n      \"注：2030年はIMF予測。EUはG7加盟国を除く。\",\n      \"資料：IMF WEO April 2025\"\n    )\n  ) +\n  theme_minimal(base_family = \"sans\")\n```\n\n::: {.cell-output-display}\n![](chapter5_2026_files/figure-html/unnamed-chunk-34-1.png){width=672}\n:::\n:::\n\n:::\n\n# 章末チェック\n\n- 図の目的と観測単位を説明できる。\n- 棒、ヒストグラム、箱ひげ、散布、折れ線を使い分けられる。\n- 軸の単位とデータ加工条件を注記している。\n- 二軸グラフに安易に頼らず、指数化やファセットを検討している。\n- ランキングと一人当たり指標の違いを説明できる。\n- 図をオブジェクトとして保存し、`ggsave()`で出力できる。\n",
    "supporting": [
      "chapter5_2026_files"
    ],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {},
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}