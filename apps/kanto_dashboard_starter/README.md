# Kanto dashboard starter

This is the Chapter 8 starter app. Chapter 7 must first create:

```text
data_clean/kanto_dashboard_data.rds
```

Run from the project root:

```r
shiny::runApp(file.path("apps", "kanto_dashboard_starter"))
```

Complete the exercises in `chapter8_2026.qmd`, then compare your implementation with `apps/kanto_dashboard/`.
